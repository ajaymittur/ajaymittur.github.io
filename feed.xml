<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://ajaymittur.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ajaymittur.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-02T19:40:15+00:00</updated><id>https://ajaymittur.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Coding Agents, Harnesses, and Where Things Are Going</title><link href="https://ajaymittur.github.io/blog/2026/coding-agents-harnesses-where-things-are-going/" rel="alternate" type="text/html" title="Coding Agents, Harnesses, and Where Things Are Going"/><published>2026-07-11T10:00:00+00:00</published><updated>2026-07-11T10:00:00+00:00</updated><id>https://ajaymittur.github.io/blog/2026/coding-agents-harnesses-where-things-are-going</id><content type="html" xml:base="https://ajaymittur.github.io/blog/2026/coding-agents-harnesses-where-things-are-going/"><![CDATA[<p><em>I do a good chunk of research on coding agents and harnesses at NVIDIA. This is my take on where the design of these systems has been heading.</em></p> <p>If you’ve used Claude Code, Codex, or OpenCode, you’ve used a <em>coding agent</em>: a model wrapped in a loop that lets it read files, run commands, and act on a repo. What’s easy to miss is that almost all of these agents make the same core bet about <em>how</em> the model acts — and that a quieter, more interesting bet lost, came back, and is now folding into the winner. This post is about that arc, and about the layer where I think most of the near-term gains actually live: the harness.</p> <h2 id="two-schools-react-and-codeact">Two schools: ReAct and CodeAct</h2> <p>Around 2023–2024, two styles of agent crystallized.</p> <p>The first is <strong>ReAct</strong> — reason, then act — from <a href="https://arxiv.org/abs/2210.03629">“ReAct: Synergizing Reasoning and Acting in Language Models”</a>. The model thinks, then emits a <em>structured tool call</em> (“open this file,” “run this command”), gets a result back, and repeats. Every production coding agent today works this way: Claude Code, Codex, OpenCode, and the research harness I work on all reason-then-emit-a-tool-call in a loop.</p> <p>The second is <strong>CodeAct</strong>, from <a href="https://arxiv.org/abs/2402.01030">“Executable Code Actions Elicit Better LLM Agents”</a> (Xingyao Wang et al., ICML 2024). Instead of picking from a fixed menu of tools, the model <em>writes and runs actual code</em> — usually Python in a live interpreter — as its action. The paper found this unified, code-native action space beat conventional tool-calling by up to ~20% on their benchmarks.</p> <p>The distinction worth being precise about: a ReAct agent with a <code class="language-plaintext highlighter-rouge">bash</code> tool can obviously run code too. The real difference is what the <em>primary action surface</em> is. In CodeAct, writing and running code is the interface itself, and the agent’s own functions become its API. That’s what makes it powerful — and, as it turns out, what made it hard to ride.</p> <h2 id="the-convergence--and-the-fold">The convergence — and the fold</h2> <p>Here’s the plot twist. OpenHands is the flagship open-source agent built by the very author of CodeAct. And OpenHands itself <a href="https://www.openhands.dev/blog/openhands-codeact-21-an-open-state-of-the-art-software-development-agent">moved from a pure CodeAct design to function calling</a> in its CodeAct 2.1 release — reporting that structured tool calls made runs smoother, resolved more issues without human intervention, and got the agent stuck in loops less often (it hit a state-of-the-art ~53% on SWE-bench Verified at the time). When the person who coined CodeAct ships the ReAct pattern, that tells you which way the training winds were blowing: frontier models were increasingly <em>post-trained</em> for structured tool calling, so the harness that matched the model won.</p> <p>But the story doesn’t end at “ReAct won.” The CodeAct <em>idea</em> — code as a dense, composable action — is quietly folding back in, just through a simpler door. Instead of a full REPL-native harness, recent agents lean on a single <strong>bash/terminal tool</strong>, and a single command can carry arbitrarily rich code. The action surface collapses back toward “the model plus a shell.”</p> <p>You can see this most clearly in how minimal the winning harnesses have become:</p> <ul> <li><strong>mini-swe-agent</strong> (from the SWE-bench / SWE-agent team) is <a href="https://github.com/SWE-agent/mini-swe-agent">~100 lines of Python, bash-only, with <em>no</em> tool-calling interface at all</a> — and still scores &gt;74% on SWE-bench Verified. It doesn’t even need the model’s function-calling API; every action is just a bash command.</li> <li><strong>Terminus</strong>, the reference agent for <a href="https://www.tbench.ai/news/terminus">Terminal-Bench</a>, is deliberately “an LLM plus a terminal” — a single tmux session as its only tool, no editing tools, no planner, no memory.</li> </ul> <p>This is the “fold” I keep coming back to: the industry converged on ReAct, and now ReAct is absorbing what made CodeAct compelling by leaning on more code execution — treating the terminal (or a code sandbox) as the universal action. Models have gotten good enough at driving a shell that a huge amount of scaffolding just… evaporates. Someone literally titled a teaching harness <a href="https://github.com/shareAI-lab/learn-claude-code">“Bash is all you need.”</a></p> <p>It’s telling that even the OpenHands founders frame it this way. When the CodeAct → tool-calling history came up, Graham Neubig pushed back on drawing too sharp a line — the terminal tool, he argues, is essentially <em>still</em> CodeAct, because it gives the agent the ability to write and run code, which is the essence of CodeAct:</p> <blockquote class="twitter-tweet"><p lang="en" dir="ltr">Thanks for the historical context! I slightly disagree on the interpretation though, I think the TerminalTool is essentially still CodeAct. It gives the agent the ability to write code and perform tasks which is the essence of CodeAct.</p>&mdash; Graham Neubig (@gneubig) <a href="https://x.com/gneubig/status/2061161541290864702?ref_src=twsrc%5Etfw">May 31, 2026</a></blockquote> <script async="" src="https://platform.x.com/widgets.js" charset="utf-8"></script> <p>Which is exactly the point: once ReAct’s “run this command” tool becomes the primary action surface and the model starts writing real code into it, the ReAct/CodeAct distinction mostly dissolves.</p> <p>You can watch this happen live. Point Codex at a task and it will often <em>write and run an ephemeral script</em> rather than make a tidy sequence of tool calls — a <code class="language-plaintext highlighter-rouge">python3 - &lt;&lt;'PY' … PY</code> heredoc that greps the tree, filters, and prints an answer in one shot:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/heredoc-codeexec-480.webp 480w,/assets/img/heredoc-codeexec-800.webp 800w,/assets/img/heredoc-codeexec-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/heredoc-codeexec.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="A terminal-tool code-execution step: a Codex-style agent writing and running a python3 heredoc script that greps the repo and prints an answer in one shot." data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">An illustrative heredoc code-execution step, of the kind Codex/GPT-style agents frequently produce.</div> <p><em>Illustrative, but this is a real and widely-observed behavior:</em> GPT/Codex frequently reach for Python (or <code class="language-plaintext highlighter-rouge">cat</code>, <code class="language-plaintext highlighter-rouge">node</code>, even Ruby) via the shell instead of a dedicated file-edit tool — to the point where people <a href="https://github.com/openai/codex/discussions/3057">debate how to stop it in <code class="language-plaintext highlighter-rouge">AGENTS.md</code></a>. It’s very likely a fingerprint of the RL environment these models were trained in, and it’s the CodeAct idea leaking out of a plain terminal tool: one code action does the work of a dozen tool calls.</p> <p>And it’s not just coding agents. The same instinct — <em>let the model write code instead of chaining tool calls</em> — is showing up across production systems. Perplexity moved search to <a href="https://www.perplexity.ai/hub/blog/sandbox-api-isolated-code-execution-for-ai-agents">“Search as Code,”</a> where an agent writes Python against low-level search primitives in a sandbox instead of looping through one function call at a time, collapsing many round trips into a single program. Glean makes the same argument for enterprise agents with <a href="https://www.glean.com/blog/harness-context-manager">“programmatic tool calling”</a>: expose tools as Python-callable functions and let the model write a small program that retrieves, filters, loops, and acts in one execution, keeping intermediate state in variables and files rather than the prompt. Both are betting that code execution — not a long chain of discrete tool calls — is the natural action surface for agents. That’s the fold generalizing well beyond the terminal.</p> <p>Why is code the natural substrate? Because it’s how you <em>compose</em>. Standard tool calling forces a model to do everything sequentially: call a tool, wait, read the result, decide the next call, repeat — one round trip at a time, with every intermediate result passing back through the context window. Code collapses that. A few lines can chain ten operations, branch on results, loop over a list, run independent calls in parallel, and keep bulky intermediate state in variables and files instead of the prompt. Tool chaining, fan-out/parallelism, map-reduce, retries — these are native to a programming language and awkward to express as a sequence of discrete tool calls. Today’s models are trained to do this stepwise via tool calling; the trajectory is toward letting them express the same logic <em>as code</em>, in one shot. That’s the real content of the ReAct↔CodeAct convergence: not “which syntax wins,” but that the action surface is moving toward code because code is simply the better substrate for composition.</p> <h2 id="the-recipe-is-commoditized-the-co-design-isnt">The recipe is commoditized; the co-design isn’t</h2> <p>Here’s a claim that would have sounded wrong two years ago: building a <em>good</em> coding agent is not that hard anymore. The recipe is well understood and increasingly public. A loop, a handful of tools (or just bash), a decent system prompt, some context management — mini-swe-agent proves you can get a strong one in ~100 lines. The barrier to a competent baseline agent has basically collapsed.</p> <p>So if the agent is easy, where does the actual work go? Two places, and they’re the whole game:</p> <p><strong>Researching and building the harness for the specific model that powers it.</strong> A harness is not model-agnostic in practice. The tools, prompts, and context strategy that make one model sing can actively hurt another — the compaction prompt, the tool descriptions, even how you format observations are all things that want to be tuned per model. The interesting work isn’t “build an agent,” it’s “given <em>this</em> model, engineer the harness around it until the pair is excellent,” and then keep re-doing that as the model changes underneath you.</p> <p>This isn’t hand-waving; models have visibly different <em>personalities</em> that the harness has to accommodate. GPT-in-Codex <a href="https://knightli.com/en/2026/05/08/codex-vs-claude-code-subagent-design/">tends to keep everything in a single main thread</a>, delegating explicitly and sparingly, while Claude leans hard on <strong>subagents</strong> — spinning up isolated, parallel workers — which is fair, since Anthropic largely popularized the pattern. Two strong models, two opposite default orchestration styles; a harness built around one can be actively wrong for the other. The effect is sharpest with smaller open models, which tend to be skewed toward the SWE / Python-repo tasks they were trained and evaluated on. Drop one into a <em>hardware</em> codebase (RTL, verification, logs) and it can flail out of the box — but with the right harness engineering around it, no post-training required, you can often get it working. I wrote a bit more about that here:</p> <blockquote class="twitter-tweet"><p lang="en" dir="ltr">You can tell a model is benchmaxxed on SWE when you ask it to do something in a GPU codebase and it starts looking for a README.md in the RTL 😭</p>&mdash; Ajay Mittur (@AjayMittur) <a href="https://x.com/AjayMittur/status/2061277025470710000?ref_src=twsrc%5Etfw">June 1, 2026</a></blockquote> <script async="" src="https://platform.x.com/widgets.js" charset="utf-8"></script> <p>This is exactly the observation the <a href="https://arxiv.org/abs/2606.09498">Self-Harness</a> authors formalize: “effective harness design is inherently model-specific.” Which is the whole point — if the right harness depends on the model, you have to own the harness to do the research at all.</p> <p><strong>Co-designing the model and the harness together.</strong> The tighter version of the above is to stop treating the model as fixed. The reason ReAct won production is precisely that frontier models were <em>post-trained</em> for structured tool calling — the model was built for the harness. The biggest gains come from closing that loop deliberately: shape the harness around the model, and shape the model (via post-training on the harness’s own traces) around the harness. Harness–model co-design, not harness <em>or</em> model.</p> <p>And this loop is a <em>ratchet</em>, which is the part I most want to get across. You add scaffolding to compensate for what the model can’t yet do — a search tool, a planning step, a verification loop. Then you post-train the model on that harness’s traces, and the model <em>internalizes the behavior</em>: it learns to do natively what the scaffolding was doing for it. Once it has, you can delete that piece of the harness — the model now handles it itself — which leaves a simpler harness, a stronger model, and room to post-train against the next layer. Scaffold, absorb, strip, repeat. So when I say “the model is eating the scaffolding” — a phrase I’m borrowing from Nicolas Bustamante’s <a href="https://www.nicolasbustamante.com/blog/llms-eat-scaffolding">“LLMs Eat Scaffolding for Breakfast”</a> — I don’t just mean models get better in general; I mean post-training <em>with the harness</em> is the mechanism that pulls harness behavior into the weights and lets you keep chipping the harness down. Bustamante’s example is vivid: Codex’s system prompt shrank from 310 lines to 104 (a 66% cut) from GPT-o3 to GPT-5, because the newer model already knew the behavior the old prompt had to spell out. Each turn of the ratchet leaves the model doing more and the harness doing less.</p> <p>Both of these are only possible if you <strong>own your harness</strong>. You can’t co-design against an interface you don’t control, and you can’t research per-model harness behavior if the agent loop, compaction, and tool dispatch are someone else’s compiled binary. This is the real argument for building your own harness rather than only consuming an off-the-shelf one: not that off-the-shelf agents are bad (they’re excellent), but that owning the harness is what turns “using an agent” into “researching agents.” The next two sections are what that ownership buys you — first by hand, then automatically.</p> <h2 id="where-the-gains-actually-live-the-harness">Where the gains actually live: the harness</h2> <p>The fold can make it sound like the harness is vanishing. It isn’t — it’s getting smaller and higher-leverage at the same time; the leverage just relocated from <em>how many tools</em> to <em>how well you feed and steer the model</em>. LangChain frames the anatomy cleanly: <a href="https://www.langchain.com/blog/the-anatomy-of-an-agent-harness">an agent is a model <em>plus a harness</em></a>, and the harness is everything that isn’t the model — tools, context management, feedback loops, orchestration. Even a “bash-only” agent has a harness; it’s just a very good, very small one.</p> <p>This is where owning the harness end to end pays off, and where I’ve spent most of my time. Here’s an illustrative result on an internal coding benchmark — the same model throughout, only the harness changing:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/harness-benchmark-480.webp 480w,/assets/img/harness-benchmark-800.webp 800w,/assets/img/harness-benchmark-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/harness-benchmark.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Bar chart of scores on an internal coding benchmark for a single fixed model. Two rounds of harness engineering more than double the score over a plain terminal agent; a fourth, speculative bar suggests a simpler harness could later match it." data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">Same model throughout; only the harness changes. The fourth bar is speculative.</div> <p>The first three bars are measured. Starting from a plain terminal agent, two rounds of harness engineering — better ways for the agent to navigate and retrieve context, alongside continual prompt and context-management work — more than doubled the score, with no model change. It’s not a coincidence that “install ripgrep” has become standard advice for coding agents: in a bash-native world, the harness’s leverage moves to <em>how efficiently the agent can navigate and retrieve context</em>, not how many bespoke tools it has.</p> <p>The clearest version of this shift is how coding agents largely <em>ditched RAG for grep</em>. The instinct a couple of years ago was to embed the whole repo into a vector database and retrieve chunks; the thing that actually won was letting the agent run keyword search — <code class="language-plaintext highlighter-rouge">grep</code>/<code class="language-plaintext highlighter-rouge">rg</code> — and read files like a developer would. The Claude Code team was blunt about it: agentic search outperformed embeddings-based retrieval “by a lot.”</p> <blockquote class="twitter-tweet"><p lang="en" dir="ltr">Boris from the Claude Code team explains why they ditched RAG for agentic discovery. <br/><br/>&quot;It outperformed everything. By a lot&quot; <a href="https://t.co/EzmpLeVixk">https://t.co/EzmpLeVixk</a> <a href="https://t.co/EFjhVNYX3A">pic.twitter.com/EFjhVNYX3A</a></p>&mdash; pash (@pashmerepat) <a href="https://x.com/pashmerepat/status/1926717705660375463?ref_src=twsrc%5Etfw">May 25, 2025</a></blockquote> <script async="" src="https://platform.x.com/widgets.js" charset="utf-8"></script> <p>It’s the same pattern as the CodeAct fold, one layer down: a heavier, bespoke subsystem (a vector index) gets replaced by the model driving a simple, general terminal primitive (search). Give a capable model good, fast, simple tools and let it navigate, rather than pre-chewing everything for it.</p> <p>Now, back to that chart’s fourth bar — the speculative one. It’s a “maybe?”, not a claim, and it points straight at a tension you may already feel: mini-swe-agent showed a strong enough model barely needs bespoke tooling, yet <em>this</em> model, right now, got better when I added file and search tools. The fourth bar is my bet that the first fact eventually swallows the second. As models get better at driving a raw shell and doing more code execution directly — and especially as they’re post-trained on this harness’s own traces — it’s plausible much of that bespoke tooling becomes unnecessary: you peel the engineered improvements back off, and a <em>simpler</em> terminal agent could match or beat the carefully hand-tuned one. I don’t know that it will, and I wouldn’t bet a number on it. But if it does, the gains didn’t vanish — they migrated out of your harness and into the model, exactly the ratchet from earlier. Either way, the lesson is to keep your harness thin enough to strip back.</p> <p>This isn’t just my anecdote. LangChain recently took their coding agent <a href="https://www.langchain.com/blog/improving-deep-agents-with-harness-engineering">from Top 30 to Top 5 on Terminal-Bench 2.0 — 52.8% to 66.5% — <em>without changing the model</em></a>, purely through harness work: better system prompts with self-verification loops, environment-aware context injection, and middleware to catch failure patterns like doom loops. Same model, +14 points, from engineering the scaffolding. Their <a href="https://www.langchain.com/blog/better-harness-a-recipe-for-harness-hill-climbing-with-evals">“harness hill-climbing with evals”</a> recipe is the disciplined version of that: source and tag evals, hold out a test set, baseline, read the traces, make one targeted harness change, re-measure. It’s unglamorous, and it’s most of the job. Owning the harness source is what makes that loop tight — you can instrument, mutate, and re-measure any layer instead of being boxed in by someone else’s abstractions.</p> <p>You can even see the harness effect <em>at the leaderboard level</em>. On <a href="https://www.tbench.ai/leaderboard/terminal-bench/2.1">Terminal-Bench</a>, the exact same model posts meaningfully different scores depending on the harness it’s run under. Here, each model scores several points higher on its own tuned harness (Codex CLI) than on the neutral Terminus-2 harness everyone else is measured on:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/harness-comparison-480.webp 480w,/assets/img/harness-comparison-800.webp 800w,/assets/img/harness-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/harness-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Terminal-Bench comparison showing each model scoring 5 to 7 points higher on its own tuned harness (Codex CLI) than on the neutral Terminus-2 harness, with no model change." data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">The same model scores several points higher on its own tuned harness than on the neutral Terminus-2 harness.</div> <p>That gap — 5 to 7 points here — is <em>pure harness</em>, no model change. It’s also why cross-harness leaderboard comparisons are slippery: labs report their headline number on their own tuned harness, which flatters it, so it isn’t directly comparable to a neutral-harness score. (Figures from public Terminal-Bench reporting and Anthropic’s Opus 4.6 system card, via <a href="https://www.morphllm.com/comparisons/codex-vs-claude-code">this roundup</a>.)</p> <p>It’d be tempting to reduce that to a tidy rule — “a model does best on its home harness.” It’s not that clean. <a href="https://artificialanalysis.ai/agents/coding-agents#harness-comparison">Artificial Analysis</a> holds Claude Opus 4.7 constant across three harnesses — OpenCode, Cursor CLI, and Claude Code — on their composite Coding Agent Index, and the ranking flips:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/harness-comparison-opus-480.webp 480w,/assets/img/harness-comparison-opus-800.webp 800w,/assets/img/harness-comparison-opus-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/harness-comparison-opus.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Artificial Analysis Coding Agent Index for a single fixed model (Opus 4.7) across three harnesses, showing about eight points of spread and Claude Code ranking last, behind OpenCode." data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption">Same model (Opus 4.7), only the harness changes. Source: Artificial Analysis Coding Agent Index.</div> <p>Eight points of spread from the harness alone — but this time Anthropic’s own Claude Code comes <em>last</em> for Opus, behind OpenCode. Put the two charts side by side and the lesson isn’t “own harness wins”; it’s that harness choice swings the <em>same</em> model by a lot, and <em>which</em> harness is best is an empirical, model-specific question you can’t predict from who built it. That’s the whole reason you can’t treat the harness as a fixed given — and why owning and tuning it is where the research actually is.</p> <h2 id="self-evolving-harnesses-the-loop-closes">Self-evolving harnesses: the loop closes</h2> <p>If the harness is this important, the natural next question is: can the agent improve its <em>own</em> harness? That used to sound speculative — the early proof-of-concepts, like <a href="https://arxiv.org/abs/2408.08435">ADAS</a> (a meta-agent programming new agents in code) and the <a href="https://arxiv.org/abs/2505.22954">Darwin Gödel Machine</a> (an agent that rewrites its own code and took <em>itself</em> from 20% to 50% on SWE-bench), are already a year or more old. It doesn’t sound speculative anymore; the recent work makes it concrete, and it maps almost exactly onto the manual loop from the last section.</p> <ul> <li><strong><a href="https://arxiv.org/abs/2606.09498">Self-Harness</a></strong> (Zhang et al., 2026) automates precisely the hill-climbing loop I described: mine model-specific failure patterns from traces, propose minimal harness edits tied to those failures, accept them only after regression testing. Starting from a <em>minimal</em> harness on Terminal-Bench-2.0, it lifted three different open models substantially — one from 40.5% to 61.9% held-out — with no human engineer and no stronger external agent in the loop.</li> <li><strong><a href="https://arxiv.org/abs/2607.03935">Harness-Aware Self-Evolving (HASE)</a></strong> (Luo et al., 2026) goes further and co-evolves the <em>model weights and the harness together</em> in one RL process — the co-design thesis made literal. A single Qwen3-8B model under HASE matched a GPT-OSS-120B model that used Claude Code as its harness proposer.</li> <li><strong><a href="https://arxiv.org/abs/2512.24601">Recursive Language Models</a></strong> (Zhang, Kraska, Khattab; MIT CSAIL) let a model treat its own prompt as an environment it can decompose and recursively call itself over — restructuring its own scaffolding at run time, pushing effective context past millions of tokens while <em>improving</em> quality.</li> </ul> <p>Lilian Weng’s recent survey, <a href="https://lilianweng.github.io/posts/2026-07-04-harness/">“Harness Engineering for Self-Improvement,”</a> is a good map of this whole space if you want to go deeper.</p> <p>The through-line: when your harness is code the agent can read and rewrite, self-improvement stops being bolted-on and becomes native. This is exactly why owning the harness matters so much. A third-party agent whose core is a compiled binary can’t introspect and rewrite itself; a harness that’s just readable, mutable source in the same process can. The manual hill-climbing loop from the last section — baseline, read traces, change the harness, re-measure — is precisely the loop Self-Harness and friends automate. Human-in-the-loop harness engineering and automated self-evolution aren’t two different bets; they’re the same loop at different levels of automation, and the hand-off between them is already happening.</p> <h2 id="where-things-are-going">Where things are going</h2> <p>My read, stitched together:</p> <p>The biggest bet I’d make is on <strong>convergence</strong>: ReAct and CodeAct are collapsing into one code-execution-centric style. Models and agents are becoming more and more code-native because <strong>code is the better substrate for composition</strong> — tool chaining, branching, parallel fan-out, map-reduce, retries — all the things models currently do stepwise, one tool call at a time, through the context window. The “won” pattern (ReAct tool calling) and the “lost” pattern (CodeAct) are meeting in the middle: a model that reasons, then <em>writes and runs code</em> as its action. That’s the direction I’d point at if you asked me where this is all going.</p> <p>Everything else follows from that. The <strong>ReAct tool-calling pattern won</strong> the first production round because models were trained for it — that model–harness coupling is the thing to respect — but the <strong>code-execution instinct is folding back in</strong>, and not just for coding agents: search and enterprise agents are making the same move, and harnesses are getting radically smaller as models get better at writing and running code directly. <strong>Building the agent got easy</strong>, so the value moved — to researching the harness for your specific model, and ultimately to <strong>co-designing the model and harness together</strong>. That co-design is a <em>ratchet</em>: scaffold to cover a model’s weakness, post-train on the harness’s traces so the model absorbs that behavior, strip the now-redundant scaffolding, repeat — the model eats the scaffolding one layer at a time. The <strong>harness is where you win or lose</strong>, and its leverage keeps shifting: from clever tool menus, to context and search (RAG → grep), to eval-driven iteration, and now to letting the agent iterate on the harness itself. <strong>Self-evolving harnesses aren’t a someday-frontier</strong> — agents are already rewriting their own code to real, measured gains; the manual and automated versions of harness engineering are converging into one loop.</p> <p>The connective tissue through all of it is ownership. You can’t co-design against a model, tune a harness per model, or let a harness rewrite itself if you don’t own the harness in the first place. Off-the-shelf coding agents are great for getting work done. But if you want to <em>move the frontier</em> rather than ride it, own your harness — engineer it, measure it, and increasingly, let it evolve itself.</p> <p>If I had to compress it to one line: the model is eating the scaffolding, so the real work is co-designing the two — own a harness you can measure, rewrite, and eventually hand the pen to.</p> <hr/> <p><em>Sources are linked inline. Views are my own. Opus 4.8 helped me refine my draft.</em></p>]]></content><author><name></name></author><category term="agents"/><category term="agents"/><category term="llm"/><category term="coding-agents"/><category term="harness-engineering"/><summary type="html"><![CDATA[Two schools of coding agents, why they're converging, and why the harness — not just the model — is where a lot of the gains now live.]]></summary></entry><entry><title type="html">MLBox: Automating Machine Learning in Python</title><link href="https://ajaymittur.github.io/blog/2024/mlbox-automating-machine-learning-in-python/" rel="alternate" type="text/html" title="MLBox: Automating Machine Learning in Python"/><published>2024-03-24T22:07:24+00:00</published><updated>2024-03-24T22:07:24+00:00</updated><id>https://ajaymittur.github.io/blog/2024/mlbox-automating-machine-learning-in-python</id><content type="html" xml:base="https://ajaymittur.github.io/blog/2024/mlbox-automating-machine-learning-in-python/"><![CDATA[]]></content><author><name></name></author></entry></feed>